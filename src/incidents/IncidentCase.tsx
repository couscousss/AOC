import { EmptyState } from '@/ui/bits';
// STUB — replaced by the full module.
export function IncidentCase({ id }: { id: string }) { return <EmptyState title={id} body="Module under construction." />; }
